on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n acrecog1 -g acrecog1a180-rg --location "UK South" --sku Basic --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n acrecog1 --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n acrecog1 --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n acrecog1 --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: acrecog1.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t acrecog1.azurecr.io/recogaks:${{ github.sha }} --label dockerfile-path=Dockerfile
        docker push acrecog1.azurecr.io/recogaks:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_recogaks_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep default | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace default`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: default
        container-registry-url: acrecog1.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: recogaksdockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: default
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          acrecog1.azurecr.io/recogaks:${{ github.sha }}
        imagepullsecrets: |
          recogaksdockerauth   
