apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-01b2"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-01b2"
  template:
    metadata:
      labels:
        app: "recogaks-01b2"
    spec:
      containers:
        - name: "recogaks-01b2"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 3000