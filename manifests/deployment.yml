apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-frontend"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-frontend"
  template:
    metadata:
      labels:
        app: "recogaks-frontend"
    spec:
      containers:
        - name: "recogaks-frontend"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 3000