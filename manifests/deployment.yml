apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-c95c"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-c95c"
  template:
    metadata:
      labels:
        app: "recogaks-c95c"
    spec:
      containers:
        - name: "recogaks-c95c"
          image: "recogacr.azurecr.io/recogaks"
          ports:
          - containerPort: 3000