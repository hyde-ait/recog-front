apiVersion: v1
kind: Service
metadata:
    name: "recogaks-c95c"
    labels:
        app: "recogaks-c95c"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-c95c"