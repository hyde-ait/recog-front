apiVersion: v1
kind: Service
metadata:
    name: "recogaks-774f"
    labels:
        app: "recogaks-774f"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-774f"