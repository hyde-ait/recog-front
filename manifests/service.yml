apiVersion: v1
kind: Service
metadata:
    name: "recogaks-01b2"
    labels:
        app: "recogaks-01b2"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-01b2"