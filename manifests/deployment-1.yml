apiVersion : apps/v1
kind: Deployment
metadata:
  name: "recogaks-b88c"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "recogaks-b88c"
  template:
    metadata:
      labels:
        app: "recogaks-b88c"
    spec:
      containers:
        - name: "recogaks-b88c"
          image: "acrecog1.azurecr.io/recogaks"
          ports:
          - containerPort: 3000