apiVersion: v1
kind: Service
metadata:
    name: "recogaks-b88c"
    labels:
        app: "recogaks-b88c"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "recogaks-b88c"